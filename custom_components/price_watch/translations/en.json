{
  "config": {
    "step": {
      "user": {
        "title": "Connect Price Watch",
        "description": "Enter the URL and scoped API token for your private Price Watch service.",
        "data": {
          "base_url": "Price Watch URL",
          "api_token": "API token"
        }
      },
      "reconfigure": {
        "title": "Reconfigure Price Watch",
        "data": {
          "base_url": "Price Watch URL",
          "api_token": "API token"
        }
      }
    },
    "error": {
      "invalid_auth": "The API token was rejected.",
      "cannot_connect": "Cannot connect to the Price Watch service.",
      "timeout": "The Price Watch service did not respond in time.",
      "invalid_response": "The Price Watch service returned an invalid health response.",
      "invalid_url": "Enter an HTTPS URL for production or non-local service endpoints. The only HTTP exceptions are local/test endpoints: use homeassistant.local for a local Home Assistant service, localhost for local use, or price-watch.test for tests. IP-literal and public HTTP destinations are rejected. Do not include credentials, query parameters, or fragments."
    },
    "abort": {
      "single_instance_allowed": "Only one Price Watch service can be configured until the API exposes a stable service instance ID.",
      "reconfigure_successful": "Price Watch was reconfigured successfully."
    }
  },
  "services": {
    "set_enabled": {
      "name": "Set watch enabled state",
      "description": "Enable or disable one Price Watch watch.",
      "fields": {
        "watch_id": {
          "name": "Watch ID",
          "description": "Immutable Price Watch watch ID."
        },
        "enabled": {
          "name": "Enabled",
          "description": "Whether Price Watch should schedule checks for this watch."
        }
      }
    },
    "set_target_price": {
      "name": "Set watch target price",
      "description": "Set one watch target price in non-negative AUD cents without running a check.",
      "fields": {
        "watch_id": {
          "name": "Watch ID",
          "description": "Immutable Price Watch watch ID."
        },
        "target_price_cents": {
          "name": "Target price in cents",
          "description": "Non-negative AUD integer cents. Idempotency is generated internally."
        }
      }
    },
    "reload_image": {
      "name": "Reload watch image",
      "description": "Refresh one cached Home Assistant image without running a watch check.",
      "fields": {
        "watch_id": {
          "name": "Watch ID",
          "description": "Immutable Price Watch watch ID."
        }
      }
    }
  }
}
